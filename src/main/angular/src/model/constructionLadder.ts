import {IConstructionLadder} from "../app/Resourcenpanel/IConstructionLadder";

export class ConstructionLadder implements IConstructionLadder{
  constructor(public firstName:string, public lastName:string){

  }
}
